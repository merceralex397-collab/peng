Repo survey: current Rust core contains only tauri::Builder.run and no domain/application/command modules. FRD-001 splits the lifecycle across LIB-001 (domain/commands) and LIB-002 (SQLite), while LIB-001 explicitly says commands must be production-wired and project conduct says registered-but-unreachable/test-only code is not Done. Planning must reconcile that boundary without an in-memory fallback or absorbing LIB-002. The bundle schema provides concrete v1 limits for name (1..300), summary (<=2000), tag (1..100, unique), asset type token, status enum, source descriptor, UUID and version descriptors. No declared external research sources exist.
